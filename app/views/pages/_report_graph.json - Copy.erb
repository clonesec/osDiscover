{ "elements": 
[ { "type": "line", "text":"Reports per day", "on-show":{"type": "pop-up", "cascade":0, "delay":0}, "colour": "#ccc", "values":[ <%= a = current_user.show_reports(Date.today - 7.days).count %>,<%= b = current_user.show_reports(Date.today - 6.days).count %>,<%= c = current_user.show_reports(Date.today - 5.days).count %>,<%= d = current_user.show_reports(Date.today - 4.days).count %>,<%= e = current_user.show_reports(Date.today - 3.days).count %>,<%= f = current_user.show_reports(Date.today - 2.days).count %>,<%= g = current_user.show_reports(Date.today - 1.days).count %>], "dot-style": { "type":"solid-dot", "colour":"#ddd", "dot-size": 6, "on-click": "report_graph_date" } }  ],
 "title": { "text": "Latest Reports", "style":"{color: #333333; font-size: 25;}" },
 	"y_axis":{
		"grid-colour": "#999999",
		"stroke": 5,
		"tick-length":  0,
		"colour": "#909090",
		"max": <%= [a,b,c,d,e,f,g].max + 1 %>
	},
  	"x_axis":{
		"3d": 15, "colour": "#909090",
		"grid-visible": false,		
		"stroke": 400,
		"labels":{  
		  "visible": true,
		  "steps":  1,
		  "labels": [<%= raw @days %>]
		}	
	},

  "bg_colour":  "#D6DEE2"
}
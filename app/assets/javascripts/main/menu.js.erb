var timeout         = 200;
var closetimer		= 0;
var ddmenuitem      = 0;
function ddm_open(){
	ddm_canceltimer();
	ddm_close();
	ddmenuitem = $(this).find('ul').eq(0).css('display', 'block');
}
function ddm_close(){if(ddmenuitem) ddmenuitem.css('display', 'none');}

function ddm_timer(){closetimer = window.setTimeout(ddm_close, timeout);}

function ddm_canceltimer(){
	if(closetimer){
		window.clearTimeout(closetimer);
		closetimer = null;
	}
}

$(document).ready(function(){
	$('#ddm > li').bind('mouseover', ddm_open);
	$('#ddm > li').bind('mouseout',  ddm_timer);
	$(".no_menu_link").click(function(event){
		event.preventDefault();
	});
});
document.onclick = ddm_close;


/*
 * For ipads
 
$(document).ready(function(){

	$("#ddm > li").click(function() {
		var menu = $(this).find('ul');
		if (menu.length > 0) {
			closeAllOtherMenus(menu);
			menu.toggle();
		}
		//return false;
	});

	//$(document).click(function() {
	//	closeAllMenus();
	//});


	$(".no_menu_link").click(function(event){
		event.preventDefault();
	});
});

function closeAllOtherMenus(menu) {
	$.each($("#ddm > li ul"), function() {
		if($(this).data("menu") != menu.data("menu")) {
			$(this).css("display", "none");
		}
	});
}

function closeAllMenus() {
		$("#ddm > li ul").css("display", "none");
}

*/

